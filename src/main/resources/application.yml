server:
  port: 8080
spring:
  datasource:
    password: password
    username: user
    url: jdbc:mysql://localhost:3306/sample
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

